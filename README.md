# Weather-widget

Weather-widget is a tool for checking weather in cpecific city.

## Installation

Use yarn to install packages.

```bash
yarn && yarn serve
```

## Requirements

```bash
npx requirements
```
